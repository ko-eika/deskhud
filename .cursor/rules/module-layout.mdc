---
description: 模块目录组织，避免 src 根散落
alwaysApply: true
---

# 模块目录

- **一能力一目录**：相关类型放同一子目录。
- **新建优先目录**：`foo/mod.rs` 或 `foo.rs` + `foo/`。
- **控制单文件体量**：职责变多时拆子模块。
- 极小单文件（薄 `lib.rs`、短 `error.rs`）可暂不平铺成目录。

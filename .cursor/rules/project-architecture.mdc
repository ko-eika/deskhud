---
description: 分层与依赖硬约束（详见 CONSTRAINTS.md）
alwaysApply: true
---

# 架构（摘要）

权威全文：`docs/agent/CONSTRAINTS.md`「分层边界」；概览见 `AGENTS.md`。

- `deskhud-egui`：唯一 UI；`deskhud-runtime`：发现/加载；`deskhud-engine`：仅契约；`packs/*`：出厂包；`deskhud-sdk`：仅社区/examples。
- 禁止第二套 UI、托盘、社区原生 dll、engine→sdk、UI→`git2`。
- 第三方版本只写根 `[workspace.dependencies]`；一能力一目录。

---
description: Rust 约定（详见 CONSTRAINTS.md）
globs: "**/*.rs"
alwaysApply: false
---

# Rust（摘要）

权威全文：`docs/agent/CONSTRAINTS.md`「通用工程」。

- `edition = "2024"`；公共 API 注明「为什么」；生产路径避免 `unwrap`。
- 依赖 `{ workspace = true }`；`cargo fmt`；尽量 `clippy -D warnings`。

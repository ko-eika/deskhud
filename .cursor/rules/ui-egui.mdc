---
description: egui 壳与窗口不变量（详见 CONSTRAINTS.md）
globs: apps/deskhud-egui/**
alwaysApply: false
---

# egui 壳（摘要）

权威全文：`docs/agent/CONSTRAINTS.md`「UI / 窗口 / HUD」。

- 设置侧栏：常规 / 宠物 / 插件 / 关于；默认常规。
- 主宠窗可透明；设置/菜单/HUD **子窗禁止** `with_transparent(true)`；deferred 仅在 `App::ui`。
- 运行态 HUD：每屏一合成窗同层绘制；启用 = master∧plugin∧item。
- 宠置顶只跟 prefs；开设置时可用点击穿透；勿每帧 WindowLevel。
- 预览/图标字段：`preview` / `icon`；禁止窗口 RGN；平台码走 `platform/`。

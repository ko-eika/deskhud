# DeskHud shell locales

The shell translation sources are TOML files kept here and loaded by `deskhud-ui`.
Package translations remain inside each pack under `i18n/` and are merged at runtime.

Gettext `.po/.mo` support is intentionally deferred to a later compatibility task.
